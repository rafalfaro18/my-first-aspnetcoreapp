# my-first-aspnetcoreapp

This is an example project to learn ASP.net .

CD into repo folder and run:
```bash
docker run -it -p 5000:80 -v $(PWD):/my-first-aspnetcoreapp --workdir /my-first-aspnetcoreapp microsoft/aspnetcore-build bash
```
